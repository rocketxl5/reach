export default function animateMenu(hamburger) {

    const middle = hamburger.firstChild
    const before = getComputedStyle(middle, ':before')
    const after = middle.afer
    // after.style.transform = 'translate(-1px, -9px;)'
    console.log(middle)
    console.log(before)
    console.log(after)
}